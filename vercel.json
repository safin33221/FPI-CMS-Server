{
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "prisma generate --schema prisma/schema && tsc",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "env": {
    "DATABASE_URL": "@database_url"
  }
}